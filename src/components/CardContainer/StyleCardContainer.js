import { StyleSheet } from 'react-native';

const StyleCardContainer = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
    overflow: 'scroll',
    marginTop: 40 
  },
});

export default StyleCardContainer;